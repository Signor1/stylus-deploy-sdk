{
  "name": "@stylus-deploy/contracts",
  "version": "0.0.0",
  "private": true,
  "description": "Smart contracts for Stylus Deploy SDK",
  "scripts": {
    "build": "echo 'Building contracts...'",
    "test": "echo 'Testing contracts...'",
    "lint": "echo 'Contracts - skipping lint (handled by Foundry/Cargo)'",
    "typecheck": "echo 'Contracts - skipping typecheck (handled by Foundry/Cargo)'",
    "clean": "rm -rf target cache out"
  }
}
